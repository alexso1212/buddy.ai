请为图谱页面实现一套基于物理引擎的力学交互系统，所有节点（包括部门大圈和任务小节点）都遵循以下物理规则：

## 核心力学模型

### 1. 排斥力（Repulsion）
- 所有物体之间存在短程排斥力，类似同极磁铁
- 两个物体越靠近，排斥力越大（指数级增长）
- 物体之间永远不会重叠或接触，始终维持一个最小间距（小节点之间最小间距约 10px，部门圈之间最小间距约 30px）
- 使用公式类似：F_repulsion = k / (distance^2)，当 distance < minGap 时力急剧增大

### 2. 集群吸引力（Cluster Gravity）
- 同一部门内的所有任务小节点之间存在弱吸引力
- 它们倾向于聚集在一起形成星群，但因为排斥力的存在不会贴到一起
- 当拖动其中一个小节点时，同部门的其他节点会被"牵引"着跟随移动，但有延迟和弹性（类似弹簧连接）
- 吸引力公式：F_attract = spring_constant * (distance - restLength)，restLength 是理想间距
- 部门大圈的位置 = 该部门所有子节点的质心（centroid），大圈半径动态适应包含所有子节点

### 3. 部门圈之间的力学
- 三个部门圈之间也存在排斥力，不会重叠
- 拖动部门圈的边框时，整个部门（大圈 + 所有子节点）一起移动
- 部门圈之间也有微弱的吸引力，防止它们飞太远散开

### 4. 惯性与阻尼（Inertia & Damping）
- 所有移动都有惯性：松手后物体会继续滑行，但迅速减速
- 阻尼系数较高（约 0.85-0.92），确保物体不会飞很远
- 速度衰减公式：velocity *= dampingFactor（每帧）
- 设置最大速度上限，防止任何物体飞出屏幕

### 5. 空闲漂浮动画（Idle Drift）
- 当用户没有任何拖拽操作时，所有节点在原位附近做缓慢的随机微漂浮
- 每个节点有独立的漂浮相位和频率，看起来像宇宙中缓慢浮动的天体
- 漂浮幅度很小（约 2-5px），速度很慢，使用 Perlin noise 或简单的 sin/cos 混合
- 一旦用户开始拖拽，漂浮动画平滑过渡到拖拽状态

## 交互规则

### 拖拽小节点（任务行星）
1. 被拖拽的节点直接跟随手指/鼠标
2. 同部门其他节点通过弹簧力被牵引跟随，有弹性延迟
3. 不同部门的节点只受排斥力影响
4. 松手后惯性滑行 + 阻尼减速 + 集群弹簧力回弹到平衡位置

### 拖拽部门大圈（按住边框）
1. 整个部门刚体移动（大圈 + 所有子节点保持相对位置）
2. 其他部门圈受排斥力影响会被推开
3. 松手后同样有惯性和阻尼

## 技术实现建议
- 使用 requestAnimationFrame 循环做物理模拟，每帧更新所有节点位置
- 力的计算用 Canvas 或直接操作 DOM/SVG 元素的 transform
- 物理循环伪代码：

```js
function physicsTick() {
  for (each node) {
    // 1. 计算排斥力（与所有其他节点）
    // 2. 计算集群吸引力（与同部门节点）
    // 3. 如果空闲，加上漂浮力
    // 4. 合力 -> 加速度 -> 更新速度
    // 5. 速度 *= damping
    // 6. clamp 速度到最大值
    // 7. 更新位置
  }
  // 更新部门大圈位置和半径（基于子节点质心和边界）
  render();
  requestAnimationFrame(physicsTick);
}

调参参考值（可微调）
	∙	repulsionStrength: 500-1000
	∙	springConstant: 0.03-0.08
	∙	springRestLength: 60-100px
	∙	dampingFactor: 0.88
	∙	maxVelocity: 8px/frame
	∙	idleDriftAmplitude: 3px
	∙	idleDriftSpeed: 0.002
	∙	minNodeGap: 10px
	∙	minClusterGap: 30px

  请确保在移动端触摸事件（touchstart/touchmove/touchend）和桌面端鼠标事件都能正常工作。整体效果应该像一个迷你宇宙模拟器，天体之间有引力和斥力，安静时缓慢漂浮，交互时有丝滑的物理反馈。
  
---

这个指令覆盖了你描述的所有行为。几个关键点提醒你注意：

**调参是关键** — 给到 Replit 之后大概率第一版手感不会完美，你需要重点调的是 damping（阻尼，控制滑多远）、springConstant（弹簧系数，控制牵引紧不紧）和 repulsionStrength（排斥力，控制推开多远）这三个值。可以让 Replit 把这些参数暴露到设置面板里方便你实时调。

**性能注意** — 如果节点数量多（比如未来超过100个），每帧计算所有节点对之间的力会变慢（O(n²)），到时候可能需要加空间分区优化（quadtree），但现在16人团队的规模完全不用担心。​​​​​​​​​​​​​​​​

## 手势冲突处理与视图控制

### 手势识别与冲突避免

核心原则：通过触摸点数量和触摸位置来区分操作意图。

#### 单指操作（1 touch point）
- 如果手指落在节点上（小节点或部门圈边框上）→ 拖拽该物体
- 如果手指落在空白区域 → 平移整个画布（pan）
- 判定方式：touchstart 时做 hitTest，检测触摸点是否命中某个节点
- 节点的触摸热区要比视觉大小稍大一些（padding 约 15px），方便手指点中

#### 双指操作（2 touch points）
- 始终识别为缩放/旋转画布，永远不触发节点拖拽
- 即使双指中的一根手指落在了节点上，也优先识别为缩放
- 实现：检测到第二根手指 touchstart 时，立即取消任何正在进行的单指拖拽操作

#### 单指 → 双指过渡（关键边界情况）
- 用户经常先放一根手指再放第二根手指来做缩放
- 处理方式：
  1. 第一根手指落下时，不要立即确认为"拖拽"
  2. 设置一个极短的延迟窗口（约 80-120ms）
  3. 如果在窗口内检测到第二根手指 → 切换为缩放模式，取消拖拽
  4. 如果窗口过后仍然只有一根手指 → 确认为拖拽或平移
  5. 或者更简单的方案：第一根手指落下后，如果移动距离 < 5px 时来了第二根手指，直接切换为缩放

#### 拖拽 vs 点击的区分
- 手指落下后移动距离 < 8px 且持续时间 < 200ms → 识别为点击（tap），触发节点详情弹窗
- 手指落下后移动距离 >= 8px → 识别为拖拽

### 缩放控制
- 双指捏合：缩小画布
- 双指张开：放大画布
- 设置缩放范围限制：最小 0.3x，最大 3x
- 缩放中心点 = 双指中点位置（不是屏幕中心）
- 桌面端支持鼠标滚轮缩放，同样以鼠标位置为缩放中心
- 缩放过程中所有物理模拟正常运行（漂浮动画不中断）

### 快速复位与全局视图

#### 方案一：双击空白区域 → 自适应全局视图（Fit All）
- 双击画布空白区域，自动缩放和平移，使所有部门圈和节点都刚好完整显示在屏幕内，并留有 padding（约 10% 边距）
- 过渡动画用 ease-out，时长约 400-500ms，丝滑过渡而不是瞬间跳转

#### 方案二：页面右下角添加两个浮动按钮（已有的样式基础上）

按钮1：「归位」按钮（十字准星图标，你截图里已经有那个 ⊕ 按钮）
- 点击后：平滑动画过渡到"自适应全局视图"，所有节点都可见
- 长按：重置所有节点到初始默认布局位置 + 重置缩放为 1x

按钮2：「聚焦」按钮（✦ 星形图标，截图里也已经有）
- 可以用来聚焦到某个特定部门
- 点击后弹出部门列表，选择后平滑缩放飞到该部门，并放大到刚好显示该部门集群
- 或者做成循环切换：每次点击飞到下一个部门

#### 方案三：在顶部或底部添加缩放指示条
- 显示当前缩放级别（如 "75%"）
- 点击这个百分比数字 → 直接重置为 100% 并居中
- 可选：添加 + / - 按钮作为缩放的替代方案（对不习惯双指操作的用户友好）

### 边界保护
- 平移画布时设置边界限制，不允许所有节点都移出屏幕外
- 至少有一个部门圈的一部分要在可视区域内
- 如果用户平移太远导致所有内容都看不到了，自动触发一个"弹回"动画（类似 iOS 的 rubber band 效果）

### 视觉反馈
- 节点被手指按住时：节点轻微放大（scale 1.15）+ 添加发光效果，让用户确认"我确实在拖这个东西"
- 画布平移/缩放时：不要有任何节点高亮，让用户确认"我在操作画布不是节点"
- 拖拽过程中被牵引的同部门节点：轻微降低透明度或添加微弱的连线高亮，暗示它们被牵引

