这个顾虑非常对，你之前那次冲突分析就是因为你自己先做了一遍梳理才避免了很多坑。这次功能更大，确实需要先让 Replit 把现有系统的”家底”交出来让我看。
给 Replit 的指令，让它生成一份现状报告给我：

我需要你生成一份完整的图谱系统现状报告，我要把这份报告给另一个 AI 看，
让它帮我规划新功能时不会破坏现有的任何效果。

请严格按照以下结构输出，不要遗漏任何一项：

---

## 1. 文件结构

列出所有与图谱相关的文件路径和每个文件的职责（一句话说明）。
包括组件、hooks、utils、types、样式文件、配置文件。

格式：


/path/to/file.tsx — 职责说明


## 2. 渲染架构

回答以下问题：
- 节点是用 SVG、Canvas 还是 DOM 渲染的？
- 连线是用什么渲染的？
- 粒子效果（Blood Vessel）是用什么渲染的？
- 这些图层的叠放顺序是什么？
- 用了哪些第三方库？（d3-force? d3-quadtree? pixi? three? 还是纯手写？）

## 3. 节点系统

对每种节点类型，列出：
- 类型名称
- 视觉样式（颜色怎么决定、大小怎么算、有什么特效）
- 交互行为（单击做什么、双击做什么、长按做什么、拖拽做什么）
- 相关的代码函数名

格式：


节点类型: task
颜色: 由 Color-By 系统决定（department/project/status/assignee/priority）
大小: getRadius() 基于 downstream count，范围 1.5-4px + MIN_HIT_RADIUS 12px
特效: SVG glow ring 脉冲（overdue=红色1s, ≤2天=橙色2.5s, blocked=红色2s呼吸）
单击: 高亮依赖链
双击: Fit All（空白区域）
长按: 打开 GraphNodeSheet
拖拽: scale 1.15 + glow 反馈，同部门节点弹簧牵引
函数: renderNode(), getRadius(), handleNodeClick()


对每种节点类型都这样列出。包括 bridge node（鬼节点）如果有的话。

## 4. 连线系统

- 连线代表什么关系？（依赖？从属？协作？）
- 连线有几种视觉样式？（普通线、高亮线、阻塞线等）
- Blood Vessel 粒子系统的具体实现：粒子从哪流到哪？速度多少？颜色怎么变？
- 相关函数名

## 5. 物理引擎

列出所有力的类型和当前参数值：


排斥力: strength=?, 公式=?
弹簧吸引力: constant=?, restLength=?
阻尼: factor=?
最大速度: ?
最小距离: ?
漂浮: amplitude=?, speed=?


以及：
- 力计算是 O(n²) 还是用了空间索引？
- 物理循环的帧率？和渲染帧率一样还是分开的？
- 当前能撑住多少节点不卡？

## 6. 聚类系统（星群/银河系）

- 聚类是怎么实现的？（bounding circle? force group?）
- 聚类可以拖拽吗？拖拽逻辑是什么？
- 聚类的标签怎么定位的？
- 聚类边框的视觉样式（颜色、透明度、渐变）

## 7. 视口控制

- 缩放：范围、中心点逻辑、手势处理
- 平移：边界限制
- Fit All：触发方式和动画参数
- 手势冲突处理：单指拖拽 vs 双指缩放的判定逻辑

## 8. UI 控件

列出图谱页面上所有的按钮、面板、设置项：
- 右下角按钮各自的功能
- GraphSettings 面板里有哪些选项
- Color-By 选择器的选项
- 任何其他 UI 控件

## 9. AI 集成

- AI 高亮系统（followUp/important/bottleneck）怎么工作？
- 截图发给 AI 分析的流程
- AI 能操作图谱吗？还是只读？

## 10. 数据模型

把当前 GraphNode 的完整 TypeScript 类型定义贴出来，
包括所有字段和注释。
同样贴出 Edge/Link 的类型定义。

## 11. 已知性能瓶颈

你观察到的当前性能问题：
- 多少节点开始卡？
- 最耗性能的是哪个部分？
- 有没有做过什么优化？

## 12. 当前未实现但代码里有预留的功能

如果有任何 TODO、placeholder、半成品代码，列出来。

---

输出格式要求：
- 用 markdown 格式
- 每个函数名都要准确，我会用来做代码定位
- 参数值要写具体数字，不要写"适当"或"合适"
- 如果某项你不确定，标注 [不确定] 而不是猜测


让 Replit 把这份报告生成出来之后，你直接把整个报告贴给我。我拿到以后会做三件事：
第一，标注哪些现有功能和新方案有冲突，给出具体的兼容方案。第二，按照它实际用的渲染方式（SVG/Canvas/混合）来决定 Canvas 迁移的策略和优先级。第三，基于它实际的物理引擎参数来写新功能的指令，确保新的视角切换、密度折叠不会和现有的力参数打架。
这样你给 Replit 的每一条指令都是我看过现有代码后写的，不会出现盲改覆盖的情况。​​​​​​​​​​​​​​​​