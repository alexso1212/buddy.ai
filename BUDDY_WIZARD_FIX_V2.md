# Buddy AI 引导弹窗修改指令（基于实际架构）

# 已知架构：App.tsx 管底部弹窗，agent.tsx 管 AI 聊天页面

# 引导弹窗应统一使用 App.tsx 的 Bottom Sheet 模式

-----

# 架构分析

```
目前 App.tsx 里的 5 个 Bottom Sheet：
  settingsOpen    → 点击头像    → z-index: 60
  contextMenu     → 长按对话    → z-index: 70
  renameModal     → 菜单"重命名" → z-index: 80
  deleteConfirm   → 菜单"删除"  → z-index: 80
  projectPicker   → 菜单"移到项目" → z-index: 80

⚠️ AI 引导弹窗（Wizard）也应该加入 App.tsx 的 Bottom Sheet 体系
   或者在 agent.tsx 中用同样的 Bottom Sheet 模式实现。
   z-index 建议用 75（在 contextMenu 和 renameModal 之间）
```

-----

# 修改方案

## 选项 A：在 agent.tsx 中实现（推荐）

引导弹窗是 AI 聊天特有的，放在 agent.tsx 里更合理。
但样式必须与 App.tsx 的 Bottom Sheet 保持一致。

## 选项 B：在 App.tsx 中统一管理

如果希望所有弹窗都由 App.tsx 集中管理，可以加一个 `wizardState` 状态。

**推荐选项 A**——在 agent.tsx 里实现，保持 App.tsx 不至于太臃肿。

-----

# 给 Replit 的完整执行 Prompt

```
请修改 AI 引导弹窗（任务创建向导 Wizard），对照 Claude iOS 的引导交互样式。

引导弹窗目前嵌在聊天消息流中（agent.tsx），需要改为 Bottom Sheet 风格。
请保持与 App.tsx 中其他 Bottom Sheet（设置面板、重命名、删除确认、项目选择器）
一致的设计模式。

===== 容器样式（与 App.tsx 其他 Bottom Sheet 统一） =====
1. 不再嵌在消息流中，改为浮动卡片，固定在聊天消息区底部（Composer 上方）
2. position: sticky 或 relative，贴在消息流底部
3. 背景 #323230（比页面 #2B2A27 略亮，比 composer #3C3B37 略暗）
4. 圆角 16px（四角都有圆角，因为不是贴屏底而是在消息流内浮动）
5. 边框 1px solid rgba(255,255,255,0.08)
6. 阴影 0 4px 20px rgba(0,0,0,0.25)
7. margin: 12px 16px
8. z-index: 不需要（它在消息流内，不是 fixed 定位）
9. 出现动画：opacity 0→1 + translateY(20px→0)，250ms ease-out

===== 导航栏（替换当前的 "1/4" 显示） =====
10. 删除当前的 emoji 图标 + "1/4" 步骤指示
11. 新导航栏高度 48px，padding 0 16px
12. 左侧依次排列：
    - < 按钮（Lucide ChevronLeft，20px，#9A9893）
      第一步时 opacity 0.3，禁用
    - 步骤文字 "1 of 4"，14px，#9A9893
    - > 按钮（Lucide ChevronRight，20px，#9A9893）
      最后一步时 opacity 0.3，禁用
13. 右侧：✕ 关闭按钮（Lucide X，20px，#9A9893），margin-left: auto
14. 点 < 回到上一步，点 > 跳到下一步（如果已填写）
15. 点 ✕ 关闭整个引导流程（回到普通聊天状态）

===== 标题区域 =====
16. 去掉 emoji 前缀（📝 等）
17. 标题字号 18px，字重 500，颜色 #ECECEC
18. padding: 4px 20px 16px 20px
19. 行高 1.4

===== 选择题步骤（最关键的改动） =====
20. 完全删除当前的「输入框 + 确认按钮」布局
21. 改为编号选项列表：
    - 每个选项高度 56px
    - padding: 0 20px
    - 左侧编号数字：15px，#7A7874，固定宽度 24px
    - 右侧选项文字：16px，#ECECEC
    - 编号与文字间距 16px
    - 每个选项顶部有分隔线：1px solid rgba(255,255,255,0.06)
    - hover 时背景 rgba(255,255,255,0.04)
    - 按下时背景 rgba(255,255,255,0.08)
    - ⚠️ 点击即选择并自动进入下一步，不需要确认按钮
22. 最后一行是自由输入：
    - 左侧 ✏️ 图标（Lucide Pencil，16px，#7A7874，宽 24px，与编号对齐）
    - 右侧是 input 输入框：
      placeholder "输入自定义内容..."，颜色 #7A7874
      字号 16px，颜色 #ECECEC
      背景透明，无边框
      按 Enter 提交

===== 纯输入题步骤（如"任务标题是什么"） =====
23. 显示一个输入框：
    margin: 0 20px，padding: 12px 14px
    背景 rgba(255,255,255,0.06)
    圆角 10px
    边框 1px solid rgba(255,255,255,0.08)
    聚焦时边框 1px solid rgba(174,86,48,0.5)（橙色）
    字号 16px，颜色 #ECECEC
    placeholder 颜色 #7A7874
24. 输入框下方显示确认按钮：
    margin: 12px 20px 16px 20px
    高度 44px
    背景 #AE5630，圆角 10px
    文字 "确认"，15px，600，白色
    输入为空时 opacity 0.4，禁用

===== 步骤切换动画 =====
25. 切换步骤时，内容区域有过渡动画：
    向前（下一步）：内容从右侧滑入 opacity 0→1 + translateX(15px→0)，200ms
    向后（上一步）：内容从左侧滑入 opacity 0→1 + translateX(-15px→0)，200ms

===== 步骤类型适配 =====
26. 需要支持两种步骤类型：
    type: 'select' → 显示编号选项列表 + 底部自由输入
    type: 'input'  → 显示输入框 + 确认按钮
27. 现有的引导步骤对应关系：
    "任务的标题是什么？"      → type: 'input'
    "选择优先级"             → type: 'select'，选项: 高/中/低/无
    "分配给谁？"             → type: 'select'，选项: 团队成员列表
    "属于哪个项目？"          → type: 'select'，选项: 项目列表
    （根据实际步骤调整）

===== 选择后的行为 =====
28. 选择题：点击某个选项 → 记录选择 → 自动切换到下一步（200ms 延迟后切换，让用户看到选中效果）
29. 输入题：输入后点确认 或 按 Enter → 记录输入 → 自动切换到下一步
30. 最后一步完成后 → 关闭引导弹窗 → 执行创建操作（与现有逻辑保持一致）
31. 任何时候点 ✕ 关闭 → 取消创建，回到普通聊天

===== 选中效果（瞬间反馈） =====
32. 用户点击某个选项时，该选项短暂高亮：
    背景变为 rgba(174,86,48,0.15)（橙色浅底）
    持续 200ms 后自动切换到下一步

===== 注意事项 =====
33. 不要用 position: fixed（不同于 App.tsx 的其他弹窗，引导卡片是在消息流内的）
34. 不需要遮罩层（它不是覆盖全屏的模态框）
35. 保持与现有 AI 对话逻辑的兼容——引导完成后的数据提交不要改
36. 如果当前引导弹窗的数据 / 状态管理逻辑在 agent.tsx 中，保留这些逻辑，只改 UI

请逐项实现，确保引导流程的功能逻辑不被破坏（只改 UI 和交互方式）。
```

-----

# 快速参照：两种步骤的视觉对比

## 选择题步骤（如”选择优先级”）

```
┌─────────────────────────────────────┐
│  < 2 of 4 >                    ✕   │
│                                     │
│  选择优先级                          │
│                                     │
│  ─────────────────────────────────  │
│  1    高优先级                       │
│  ─────────────────────────────────  │
│  2    中优先级                       │
│  ─────────────────────────────────  │
│  3    低优先级                       │
│  ─────────────────────────────────  │
│  4    无优先级                       │
│  ─────────────────────────────────  │
│  ✏️    自定义...                     │
└─────────────────────────────────────┘
```

## 纯输入步骤（如”任务标题”）

```
┌─────────────────────────────────────┐
│  < 1 of 4 >                    ✕   │
│                                     │
│  任务的标题是什么？                   │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ 输入任务标题...                │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │            确认                │  │  ← 橙色 #AE5630
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

-----

# 检查清单

```
【容器】
□ 背景 #323230 实底（不是描边轮廓）
□ 圆角 16px
□ 在消息流内浮动（不是 fixed 定位）
□ 有出现动画

【导航栏】
□ 左侧 < 1 of 4 > 带箭头
□ 第一步 < 灰掉，最后一步 > 灰掉
□ 右侧有 ✕ 关闭按钮
□ 不再显示 emoji + "1/4" 的旧格式

【选择题】
□ 是编号列表（1 2 3 4），不是输入框
□ 每项 56px 高，有分隔线
□ 点击即选择，不需要确认按钮
□ 底部有 ✏️ 自由输入行
□ 选中时有橙色浅底瞬间反馈

【输入题】
□ 有输入框（圆角 10px，聚焦橙色边框）
□ 有橙色确认按钮
□ 空输入时按钮禁用（opacity 0.4）

【动画】
□ 步骤切换有方向性滑动动画
□ 卡片出现有 slideUp + fade 动画

【功能】
□ 创建流程的数据提交逻辑未被破坏
□ 关闭后回到普通聊天状态
```