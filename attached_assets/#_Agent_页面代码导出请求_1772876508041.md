# Agent 页面代码导出请求  
  
> 请把以下文件的**完整代码**输出到一个文件 `AGENT_CODE_EXPORT.md` 中，每个文件用 `## 文件名` 分隔。  
  
## 需要导出的文件  
  
### 核心页面  
  
1. `client/src/pages/agent.tsx` — Agent聊天主页面  
  
### AI 相关组件（在 client/src/components/ 中搜索）  
  
1. 所有和 AI 消息渲染相关的组件（如 AiMessageBubble, MessageContent, ChatMessage 等）  
1. AiInputBar.tsx — 输入栏组件  
1. ArtifactPanel.tsx — Artifact 面板（如果有）  
1. 所有和思考过程(thinking)显示相关的组件  
  
### 服务端 AI 相关  
  
1. `server/services/ai/index.ts` — AI 服务核心（只要前 100 行 + chatStream 函数签名 + SSE 事件类型定义部分）  
1. `server/services/ai/prompts.ts` — 前 50 行（看 system prompt 结构）  
  
### 样式相关  
  
1. `client/src/index.css` 或 `globals.css` — 全局样式（只要和聊天相关的 CSS 变量和暗色主题部分）  
  
### 辅助信息  
  
1. `client/src/` 目录结构（2层深度）  
1. 列出所有 `client/src/components/` 下的文件名  
  
## 输出格式  
  
```markdown  
# AGENT_CODE_EXPORT.md  
  
## 1. client/src/pages/agent.tsx  
```tsx  
完整代码  
```  
  
## 2. client/src/components/ai/AiInputBar.tsx  
  
```tsx  
完整代码  
```  
  
…以此类推  
  
```  
## 重要  
- 不需要 node_modules 中的任何文件  
- 不需要 .bak 备份文件  
- 完整输出，不要截断  
- 如果文件超过 500 行，标注行号范围即可  
```  
