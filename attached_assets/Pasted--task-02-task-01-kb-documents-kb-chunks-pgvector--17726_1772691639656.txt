# task-02: 文件上传 + 文本提取 + 切片管道

> **前置条件**：task-01 完成（kb_documents + kb_chunks 表已创建，pgvector 已启用）
> **预计耗时**：30 分钟
> **新增文件**：`server/services/kb/` 目录（3 个新文件）
> **改动文件**：`server/routes.ts`（新增约 120 行路由）、`package.json`（新增 pdf-parse）
> **风险等级**：低（新增文件和路由，不改现有代码）

-----

## 目标

实现完整的文件处理管道：上传文件 → 提取文本 → 智能切片 → 存入 kb_chunks。

-----

## STEP 1：安装 pdf-parse

```bash
npm install pdf-parse
```

验证：`npm list pdf-parse` 应显示已安装。

-----

## STEP 2：创建 `server/services/kb/` 目录

在 `server/services/` 下新建 `kb/` 目录，和现有的 `ai/` 目录平级：

```
server/services/
├── ai/          # 现有的 AI 服务
└── kb/          # 新增的知识库服务
    ├── extractText.ts
    ├── chunkText.ts
    └── processDocument.ts
```

-----

## STEP 3：创建 `server/services/kb/extractText.ts`

```typescript
import fs from 'fs';
import path from 'path';
import mammoth from 'mammoth';

/**
 * 从文件中提取纯文本内容
 * 支持格式：PDF, DOCX, TXT, MD
 */
export async function extractText(filePath: string, fileType: string): Promise<string> {
  const absolutePath = path.resolve(filePath);
  
  if (!fs.existsSync(absolutePath)) {
    throw new Error(`文件不存在: ${absolutePath}`);
  }

  switch (fileType.toLowerCase()) {
    case 'pdf': {
      const pdfParse = (await import('pdf-parse')).default;
      const dataBuffer = fs.readFileSync(absolutePath);
      const data = await pdfParse(dataBuffer);
      return data.text || '';
    }

    case 'docx': {
      const result = await mammoth.extractRawText({ path: absolutePath });
      return result.value || '';
    }

    case 'txt':
    case 'md': {
      return fs.readFileSync(absolutePath, 'utf-8');
    }

    default:
      throw new Error(`不支持的文件类型: ${fileType}`);
  }
}
```

**说明**：

- mammoth 已安装（项目现有依赖），直接 import
- pdf-parse 用动态 import（`await import()`），因为它是新安装的
- 不处理 xlsx（后续可扩展）

-----

## STEP 4：创建 `server/services/kb/chunkText.ts`

```typescript
/**
 * 将长文本切分成适合 embedding 的片段
 * 
 * 策略：
 * 1. 先按段落分割（\n\n）
 * 2. 将段落累积到 maxChars 为止
 * 3. 切片之间保留 overlap 重叠（提高检索连续性）
 * 4. 过滤掉过短的切片
 */

interface ChunkOptions {
  maxTokens?: number;    // 每个切片最大 token 数，默认 500
  overlap?: number;      // 重叠 token 数，默认 50
  minChunkLength?: number; // 最短切片字符数，低于此值丢弃，默认 30
}

interface Chunk {
  content: string;
  index: number;
  tokenCount: number;
}

// 粗略估算 token 数：中文约 1.5 字符/token，英文约 4 字符/token
// 混合内容取中间值 2 字符/token
function estimateTokens(text: string): number {
  // 检测中文占比
  const chineseChars = (text.match(/[\u4e00-\u9fff]/g) || []).length;
  const totalChars = text.length;
  const chineseRatio = totalChars > 0 ? chineseChars / totalChars : 0;
  
  // 中文多时用更低的字符/token 比率
  const charsPerToken = chineseRatio > 0.3 ? 1.5 : chineseRatio > 0.1 ? 2 : 4;
  return Math.ceil(totalChars / charsPerToken);
}

export function chunkText(text: string, options: ChunkOptions = {}): Chunk[] {
  const maxTokens = options.maxTokens || 500;
  const overlap = options.overlap || 50;
  const minChunkLength = options.minChunkLength || 30;

  // 估算字符数限制
  const chineseChars = (text.match(/[\u4e00-\u9fff]/g) || []).length;
  const chineseRatio = text.length > 0 ? chineseChars / text.length : 0;
  const charsPerToken = chineseRatio > 0.3 ? 1.5 : chineseRatio > 0.1 ? 2 : 4;
  
  const maxChars = Math.floor(maxTokens * charsPerToken);
  const overlapChars = Math.floor(overlap * charsPerToken);

  // 按段落分割（双换行或多换行）
  const paragraphs = text.split(/\n\s*\n/).map(p => p.trim()).filter(p => p.length > 0);

  if (paragraphs.length === 0) return [];

  const chunks: Chunk[] = [];
  let currentChunk = '';
  let chunkIndex = 0;

  for (const para of paragraphs) {
    // 如果单个段落就超过 maxChars，需要强制按句子切割
    if (para.length > maxChars) {
      // 先保存当前累积的内容
      if (currentChunk.length >= minChunkLength) {
        chunks.push({
          content: currentChunk.trim(),
          index: chunkIndex++,
          tokenCount: estimateTokens(currentChunk),
        });
      }
      
      // 按句子切割超长段落
      const sentences = para.split(/(?<=[。！？.!?\n])\s*/);
      currentChunk = '';
      
      for (const sentence of sentences) {
        if (currentChunk.length + sentence.length > maxChars && currentChunk.length > 0) {
          chunks.push({
            content: currentChunk.trim(),
            index: chunkIndex++,
            tokenCount: estimateTokens(currentChunk),
          });
          // 保留 overlap
          currentChunk = currentChunk.slice(-overlapChars) + sentence;
        } else {
          currentChunk += (currentChunk ? ' ' : '') + sentence;
        }
      }
      continue;
    }

    // 正常累积段落
    if (currentChunk.length + para.length + 2 > maxChars && currentChunk.length > 0) {
      chunks.push({
        content: currentChunk.trim(),
        index: chunkIndex++,
        tokenCount: estimateTokens(currentChunk),
      });
      // 保留 overlap：取当前 chunk 末尾的 overlapChars 个字符
      const overlapText = currentChunk.slice(-overlapChars);
      currentChunk = overlapText + '\n\n' + para;
    } else {
      currentChunk += (currentChunk ? '\n\n' : '') + para;
    }
  }

  // 最后一个 chunk
  if (currentChunk.trim().length >= minChunkLength) {
    chunks.push({
      content: currentChunk.trim(),
      index: chunkIndex++,
      tokenCount: estimateTokens(currentChunk),
    });
  }

  return chunks;
}
```

-----

## STEP 5：创建 `server/services/kb/processDocument.ts`

这是核心处理管道，在文件上传后**异步**调用。

```typescript
import { storage } from '../../storage';
import { extractText } from './extractText';
import { chunkText } from './chunkText';

/**
 * 异步处理文档：提取文本 → 切片 → 存入 kb_chunks
 * 
 * 调用方式：文件上传 API 返回后，用 setTimeout 或 setImmediate 异步触发
 * 不要在 API 请求中同步执行，否则大文件会超时
 */
export async function processDocument(documentId: number): Promise<void> {
  try {
    // 1. 获取文档信息
    const doc = await storage.getKbDocumentById(documentId);
    if (!doc) {
      console.error(`[KB] Document not found: ${documentId}`);
      return;
    }

    // 2. 更新状态为 processing
    await storage.updateKbDocument(documentId, { status: 'processing' });
    console.log(`[KB] Processing document: ${doc.title} (${doc.fileType})`);

    // 3. 提取文本
    // fileUrl 格式为 /uploads/kb/xxx.pdf，需要去掉开头的 /
    const filePath = doc.fileUrl.startsWith('/') ? doc.fileUrl.slice(1) : doc.fileUrl;
    
    let rawText: string;
    try {
      rawText = await extractText(filePath, doc.fileType);
    } catch (err: any) {
      console.error(`[KB] Text extraction failed for ${doc.fileName}:`, err.message);
      await storage.updateKbDocument(documentId, {
        status: 'error',
        errorMessage: `文本提取失败: ${err.message}`,
      });
      return;
    }

    // 4. 检查提取结果
    if (!rawText || rawText.trim().length < 10) {
      await storage.updateKbDocument(documentId, {
        status: 'error',
        errorMessage: '无法从文件中提取到有效文本内容',
      });
      return;
    }

    console.log(`[KB] Extracted ${rawText.length} chars from ${doc.fileName}`);

    // 5. 切片
    const chunks = chunkText(rawText, {
      maxTokens: 500,
      overlap: 50,
      minChunkLength: 30,
    });

    if (chunks.length === 0) {
      await storage.updateKbDocument(documentId, {
        status: 'error',
        errorMessage: '文本切片后无有效内容',
      });
      return;
    }

    console.log(`[KB] Created ${chunks.length} chunks from ${doc.fileName}`);

    // 6. 先删除旧切片（如果是重新处理）
    await storage.deleteKbChunksByDocument(documentId);

    // 7. 批量插入切片（不含 embedding，embedding 在 task-03 中处理）
    const chunkRecords = chunks.map(chunk => ({
      documentId: documentId,
      orgId: doc.orgId,
      chunkIndex: chunk.index,
      content: chunk.content,
      tokenCount: chunk.tokenCount,
      metadata: JSON.stringify({
        sourceFile: doc.fileName,
        sourceTitle: doc.title,
        category: doc.category,
      }),
    }));

    await storage.createKbChunks(chunkRecords);

    // 8. 更新文档状态
    await storage.updateKbDocument(documentId, {
      status: 'ready',
      chunkCount: chunks.length,
    });

    console.log(`[KB] Document ready: ${doc.title} (${chunks.length} chunks)`);

  } catch (err: any) {
    console.error(`[KB] Processing error for document ${documentId}:`, err.message);
    try {
      await storage.updateKbDocument(documentId, {
        status: 'error',
        errorMessage: `处理失败: ${err.message}`,
      });
    } catch {
      // 如果连更新状态都失败了，只能 log
      console.error(`[KB] Failed to update error status for document ${documentId}`);
    }
  }
}
```

-----

## STEP 6：创建 `uploads/kb/` 目录

知识库文件单独存放，和交付物文件分开：

```bash
mkdir -p uploads/kb
```

-----

## STEP 7：在 routes.ts 中新增知识库路由

在 `server/routes.ts` 中，找到现有路由的末尾区域（建议放在 Token Usage 路由之后、`export` 之前），新增以下路由。

**关键**：复用现有的 `multer` 配置，但文件存储目录改为 `uploads/kb/`。

```typescript
// ===================== Knowledge Base =====================

// 知识库专用的 multer 配置（存到 uploads/kb/ 目录）
const kbUploadStorage = multer.diskStorage({
  destination: (_req: any, _file: any, cb: any) => cb(null, 'uploads/kb/'),
  filename: (_req: any, file: any, cb: any) => {
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1e9);
    const ext = pathModule.extname(file.originalname);
    cb(null, uniqueSuffix + ext);
  },
});
const kbUpload = multer({
  storage: kbUploadStorage,
  limits: { fileSize: 50 * 1024 * 1024 }, // 50MB
  fileFilter: (_req: any, file: any, cb: any) => {
    const allowedTypes = ['.pdf', '.docx', '.txt', '.md'];
    const ext = pathModule.extname(file.originalname).toLowerCase();
    if (allowedTypes.includes(ext)) {
      cb(null, true);
    } else {
      cb(new Error(`不支持的文件类型: ${ext}。支持格式: PDF, DOCX, TXT, MD`));
    }
  },
});
```

然后是路由定义：

```typescript
// 需要在文件顶部加入 import
// import { processDocument } from './services/kb/processDocument';
// import { insertKbDocumentSchema } from '@shared/schema';

// --- 上传文档 ---
app.post("/api/kb/documents/upload", authMiddleware, kbUpload.single('file'), async (req: any, res) => {
  try {
    // 权限检查：仅 owner 和 admin 可上传
    const userRole = req.userRole || 'member';
    if (!['owner', 'admin'].includes(userRole)) {
      return res.status(403).json({ error: '仅管理员可上传知识库文档' });
    }

    if (!req.file) {
      return res.status(400).json({ error: '请选择要上传的文件' });
    }

    const orgId = req.orgId;
    const userId = req.currentUserId;
    const file = req.file;
    const ext = pathModule.extname(file.originalname).toLowerCase().replace('.', '');

    // 创建文档记录
    const docData = {
      orgId,
      uploadedBy: userId,
      title: req.body.title || file.originalname.replace(/\.[^/.]+$/, ''), // 去掉扩展名作为标题
      fileName: file.originalname,
      fileType: ext,
      fileSize: file.size,
      fileUrl: `/uploads/kb/${file.filename}`,
      category: req.body.category || 'general',
      visibility: req.body.visibility || 'org',
      visibleDeptIds: req.body.visibleDeptIds || null,
      status: 'pending',
      chunkCount: 0,
    };

    const doc = await storage.createKbDocument(docData);

    // 记录操作日志
    await storage.createActivityLog({
      orgId,
      userId,
      entityType: 'kb_document',
      entityId: doc.id,
      action: 'upload',
      changes: JSON.stringify({ title: doc.title, fileName: doc.fileName, fileType: doc.fileType }),
      source: 'manual',
    });

    // 异步触发文档处理（不阻塞响应）
    setImmediate(() => {
      processDocument(doc.id).catch(err => {
        console.error(`[KB] Async processing failed for document ${doc.id}:`, err);
      });
    });

    return res.status(201).json({ data: doc });
  } catch (e: any) {
    return res.status(500).json({ error: e.message });
  }
});

// --- 获取文档列表 ---
app.get("/api/kb/documents", authMiddleware, async (req: any, res) => {
  try {
    const orgId = req.orgId;
    const docs = await storage.getKbDocumentsByOrg(orgId);
    
    // 权限过滤：member 只能看到 visibility='org' 的文档
    const userRole = req.userRole || 'member';
    const filteredDocs = ['owner', 'admin'].includes(userRole) 
      ? docs 
      : docs.filter((d: any) => d.visibility === 'org');
    
    return res.json({ data: filteredDocs });
  } catch (e: any) {
    return res.status(500).json({ error: e.message });
  }
});

// --- 获取单个文档详情 ---
app.get("/api/kb/documents/:id", authMiddleware, async (req: any, res) => {
  try {
    const id = parseInt(req.params.id);
    const doc = await storage.getKbDocumentById(id);
    if (!doc) return res.status(404).json({ error: '文档不存在' });
    if (doc.orgId !== req.orgId) return res.status(403).json({ error: '无权访问' });
    return res.json({ data: doc });
  } catch (e: any) {
    return res.status(500).json({ error: e.message });
  }
});

// --- 获取文档的切片列表 ---
app.get("/api/kb/documents/:id/chunks", authMiddleware, async (req: any, res) => {
  try {
    const id = parseInt(req.params.id);
    const doc = await storage.getKbDocumentById(id);
    if (!doc) return res.status(404).json({ error: '文档不存在' });
    if (doc.orgId !== req.orgId) return res.status(403).json({ error: '无权访问' });
    
    const chunks = await storage.getKbChunksByDocument(id);
    return res.json({ data: chunks });
  } catch (e: any) {
    return res.status(500).json({ error: e.message });
  }
});

// --- 获取文档处理状态 ---
app.get("/api/kb/documents/:id/status", authMiddleware, async (req: any, res) => {
  try {
    const id = parseInt(req.params.id);
    const doc = await storage.getKbDocumentById(id);
    if (!doc) return res.status(404).json({ error: '文档不存在' });
    
    return res.json({
      data: {
        status: doc.status,
        chunkCount: doc.chunkCount,
        errorMessage: doc.errorMessage,
      }
    });
  } catch (e: any) {
    return res.status(500).json({ error: e.message });
  }
});

// --- 删除文档 ---
app.delete("/api/kb/documents/:id", authMiddleware, async (req: any, res) => {
  try {
    const id = parseInt(req.params.id);
    const userRole = req.userRole || 'member';
    if (!['owner', 'admin'].includes(userRole)) {
      return res.status(403).json({ error: '仅管理员可删除知识库文档' });
    }
    
    const doc = await storage.getKbDocumentById(id);
    if (!doc) return res.status(404).json({ error: '文档不存在' });
    if (doc.orgId !== req.orgId) return res.status(403).json({ error: '无权访问' });

    // 删除物理文件
    const filePath = doc.fileUrl.startsWith('/') ? doc.fileUrl.slice(1) : doc.fileUrl;
    const fs = await import('fs');
    if (fs.existsSync(filePath)) {
      fs.unlinkSync(filePath);
    }

    // 删除数据库记录（CASCADE 会自动删除 chunks）
    await storage.deleteKbDocument(id);

    // 记录操作日志
    await storage.createActivityLog({
      orgId: req.orgId,
      userId: req.currentUserId,
      entityType: 'kb_document',
      entityId: id,
      action: 'delete',
      changes: JSON.stringify({ title: doc.title, fileName: doc.fileName }),
      source: 'manual',
    });

    return res.json({ data: { success: true } });
  } catch (e: any) {
    return res.status(500).json({ error: e.message });
  }
});

// --- 重新处理文档（出错后重试）---
app.post("/api/kb/documents/:id/reprocess", authMiddleware, async (req: any, res) => {
  try {
    const id = parseInt(req.params.id);
    const userRole = req.userRole || 'member';
    if (!['owner', 'admin'].includes(userRole)) {
      return res.status(403).json({ error: '仅管理员可操作' });
    }

    const doc = await storage.getKbDocumentById(id);
    if (!doc) return res.status(404).json({ error: '文档不存在' });
    if (doc.orgId !== req.orgId) return res.status(403).json({ error: '无权访问' });

    // 重置状态
    await storage.updateKbDocument(id, { status: 'pending', errorMessage: null, chunkCount: 0 });

    // 异步重新处理
    setImmediate(() => {
      processDocument(id).catch(err => {
        console.error(`[KB] Reprocess failed for document ${id}:`, err);
      });
    });

    return res.json({ data: { success: true, message: '已开始重新处理' } });
  } catch (e: any) {
    return res.status(500).json({ error: e.message });
  }
});
```

-----

## STEP 8：添加 import 语句

在 `server/routes.ts` 文件顶部的 import 区域，添加：

```typescript
import { processDocument } from './services/kb/processDocument';
```

在 import `@shared/schema` 的那行中，添加 `insertKbDocumentSchema`：

```typescript
import {
  // ... 现有的 import ...
  insertKbDocumentSchema,
} from "@shared/schema";
```

-----

## STEP 9：为 uploads/kb 目录添加静态文件服务

在 routes.ts 中现有的 `app.use('/uploads', express.static('uploads'));` 这行已经覆盖了 `/uploads/kb/` 路径（因为 `uploads/kb/` 是 `uploads/` 的子目录），所以**不需要额外添加静态文件服务**。

验证：上传文件后，访问 `http://你的域名/uploads/kb/文件名.pdf` 应该能下载到文件。

-----

## STEP 10：处理 authMiddleware 中的 userRole

检查现有的 `server/middleware/auth.ts` 中，`authMiddleware` 是否在 `req` 对象上设置了 `userRole`。

如果 JWT payload 中包含 `role` 字段（根据 HANDOVER.md，JWT payload 是 `{ userId, orgId, role }`），那么 `auth.ts` 中应该有类似：

```typescript
req.userRole = decoded.role;
```

如果没有，需要在 authMiddleware 中添加这一行。或者在路由中改为从数据库查询用户 role：

```typescript
const user = await storage.getUserById(req.currentUserId);
const userRole = user?.role || 'member';
```

**选择最简单的方式**。如果 `req.userRole` 已经存在就直接用，不存在就从数据库查。

-----

## 验证清单

完成后逐项检查：

- [ ] `npm list pdf-parse` 显示已安装
- [ ] `uploads/kb/` 目录存在
- [ ] `server/services/kb/` 目录下有 3 个文件
- [ ] `npm run dev` 启动正常

### 功能验证（用 curl 或 Postman 或浏览器 console）

**测试 1：上传 TXT 文件**

```bash
# 先创建一个测试文件
echo "这是第一章的内容。公司成立于2020年。

这是第二章的内容。我们的使命是用AI提升企业效率。

这是第三章的内容。团队目前有16个人。" > /tmp/test.txt

# 上传（替换 TOKEN 和 URL）
curl -X POST "http://localhost:5000/api/kb/documents/upload" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -F "file=@/tmp/test.txt" \
  -F "title=测试文档" \
  -F "category=general"
```

预期返回：

```json
{
  "data": {
    "id": 1,
    "title": "测试文档",
    "fileName": "test.txt",
    "fileType": "txt",
    "status": "pending",
    ...
  }
}
```

**测试 2：检查处理状态（等 2-3 秒后）**

```bash
curl "http://localhost:5000/api/kb/documents/1/status" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

预期返回：

```json
{
  "data": {
    "status": "ready",
    "chunkCount": 3,
    "errorMessage": null
  }
}
```

**测试 3：查看切片内容**

```bash
curl "http://localhost:5000/api/kb/documents/1/chunks" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

预期：返回 3 个 chunk，每个包含 content、chunkIndex、tokenCount。

**测试 4：上传一份真实的 PDF 或 DOCX 文件**

用你们 Deltapex 的真实文档测试（员工手册、制度文件等）：

- 上传后等几秒
- 检查 status 是否变为 ‘ready’
- 检查 chunkCount 是否合理（一般每页 2-5 个 chunk）
- 查看 chunks 确认文本内容正确提取

**测试 5：权限测试**

用 member 角色的账号尝试上传，应返回 403。

**测试 6：删除文档**

```bash
curl -X DELETE "http://localhost:5000/api/kb/documents/1" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

预期：

- 返回 `{ data: { success: true } }`
- 数据库中 kb_documents 和 kb_chunks 的对应记录都被删除
- 物理文件被删除

-----

## 给 Replit Agent 的指令

> 请阅读项目根目录的 `task-02-kb-pipeline.md`，按步骤执行。
> 
> 重要规则：
> 
> 1. 新增 `server/services/kb/` 目录和 3 个文件，不修改现有 AI 服务代码
> 1. 路由代码风格参照现有的交付物上传路由（`/api/tasks/:taskId/deliverables`）
> 1. multer 配置复用现有模式，但存储路径改为 `uploads/kb/`
> 1. `processDocument` 必须异步执行（setImmediate），不能阻塞 upload API 响应
> 1. 处理完成后运行 `npm run dev`，用测试文件验证上传 → 处理 → 切片完整流程
> 1. 完成后告诉我验证清单的每一项是否通过

-----

## 完成后告诉 Claude

回来告诉我：

1. “task-02 完成” 或 “task-02 有问题：XXX”
1. 上传一份真实文档后的 chunkCount 是多少（让我判断切片粒度是否合适）
1. 有没有遇到 pdf-parse 或 mammoth 的问题

然后我出 task-03（embedding 生成 + 向量检索 + 全文搜索混合）。