这个思路非常对，这才是图谱真正能 scale 的方式。给你写指令：

给 Replit 的指令：

## 图谱分层钻取系统（Hierarchical Drill-Down）

### 核心概念

图谱采用无限层级嵌套结构，用户通过点击节点"钻入"查看内部子结构，就像从宇宙视角逐层放大进入银河系→星系→恒星系统。

层级示例：
- Level 0（宇宙视角）：所有部门（市场运营、技术开发、管理层...）
- Level 1（银河系视角）：某部门内的所有项目
- Level 2（星系视角）：某项目内的所有子项目/模块
- Level 3（恒星系视角）：某子项目内的所有具体任务
- Level N：可以无限嵌套下去

### 数据结构

每个节点需要支持嵌套 children：
```ts
interface GraphNode {
  id: string;
  title: string;
  type: 'department' | 'project' | 'subproject' | 'task' | 'subtask';
  status: 'not_started' | 'in_progress' | 'completed' | 'overdue' | 'blocked';
  children?: GraphNode[];       // 下一层级的子节点
  parentId?: string;            // 父节点ID，用于向上导航
  assignees?: string[];         // 相关人员
  dependencies?: string[];      // 依赖关系（跨节点连线）

  // 聚合状态（从子节点汇总上来，用于高层级视觉提示）
  aggregatedStatus: {
    total: number;              // 子节点总数
    overdue: number;            // 逾期数量
    blocked: number;            // 阻塞数量
    completionRate: number;     // 完成率 0-1
    hasUrgent: boolean;         // 是否包含紧急问题
    deepestIssueLevel: number;  // 问题存在的最深层级
  };
}


钻取交互
进入下一层（Drill In）
  ∙	触发方式：双击节点（单击是选中/拖拽，双击是钻入）
  ∙	或者：单击节点弹出简要信息卡片，卡片上有”进入”按钮
  ∙	动画效果：
  1.	被点击的节点开始放大，占据整个屏幕
  2.	其他同级节点淡出并向四周散开
  3.	放大过程中，被点击节点的外壳（圆圈边框）逐渐淡化消失
  4.	内部的子节点从中心向外扩散展开，形成新的力导向布局
  5.	整个过渡动画 500-700ms，ease-in-out
  6.	过渡完成后，子节点开始正常的物理模拟（漂浮、互斥、吸引等）
返回上一层（Drill Out）
  ∙	动画是钻入的反向：当前层级的节点向中心收缩聚拢，父级节点的外壳重新出现，同级其他节点从四周淡入归位
如果节点没有 children
  ∙	双击后不钻入，而是打开任务详情面板（侧边滑出或底部弹出）
高层级视觉状态提示
在高层级（宇宙/银河系视角）看不到具体任务，但需要通过视觉效果传达内部状态：
节点颜色（基于 aggregatedStatus）
  ∙	健康（completionRate > 0.7 且 overdue == 0）：节点发出柔和的绿色光晕
  ∙	一般（completionRate 0.3-0.7）：节点发出黄色光晕
  ∙	危险（overdue > 0 或 blocked > 0）：节点发出红色光晕
  ∙	完成（completionRate == 1）：节点发出蓝白色光晕，亮度较低
脉冲闪烁（表示紧急程度）
  ∙	没有问题：无脉冲，静态光晕
  ∙	有逾期任务：节点缓慢脉冲（呼吸灯效果，周期约 3 秒）
  ∙	有阻塞任务：节点快速脉冲（周期约 1.5 秒）
  ∙	有紧急+阻塞：节点快速脉冲 + 边框闪烁红色
节点大小
  ∙	节点大小 = 基础大小 + (children数量 * 缩放因子)
  ∙	子节点越多的节点视觉上越大，像更大的星球
完成度环
  ∙	每个含 children 的节点外围有一圈弧形进度环
  ∙	绿色弧度 = completionRate
  ∙	红色弧度 = overdueRate
  ∙	灰色弧度 = 剩余未开始
微缩预览（可选增强）
  ∙	鼠标悬停（桌面端）或长按（移动端）某个节点时
  ∙	在节点旁边浮现一个小的半透明预览窗，显示内部子结构的缩略图
  ∙	让用户不用钻入就能快速预览内部情况
导航系统
面包屑导航栏（Breadcrumb）
在图谱顶部（标题 “Buddy” 下方）显示当前层级路径：

全局 > 技术开发 > Buddy V2 项目 > 前端模块


  ∙	每一级都可以点击，点击后直接跳转到该层级（带动画）
  ∙	“全局” 始终存在，点击 = 一键回到最顶层
  ∙	面包屑太长时左侧折叠为 “…” ，点击展开
右下角按钮更新
按钮1 ⊕（归位按钮）：
  ∙	单击：自适应当前层级的全局视图（Fit All，让当前层级所有节点都可见）
  ∙	长按：一键飞回最顶层 Level 0 全局视图
按钮2 ✦（聚焦按钮）：
  ∙	单击：弹出当前层级的节点列表，选择后飞到该节点并居中放大
  ∙	如果在高层级，列表显示部门；如果在项目层级，列表显示子项目
新增 按钮3 ←（返回按钮）：
  ∙	显示在左下角或面包屑旁边
  ∙	单击：返回上一层级（Drill Out）
  ∙	也支持手势：从屏幕左边缘右滑 = 返回上一层（类似 iOS 返回手势）
AI 上下文作用域（Context Scoping）
这是关键功能：AI 对话时只能看到与当前图谱视角相关的数据。
作用域规则
当用户在图谱中处于某个层级并唤起 AI 对话时：

interface AIContext {
  currentNode: GraphNode;           // 当前所在的节点
  children: GraphNode[];            // 当前节点的所有直接子节点（含状态）
  deepChildren?: GraphNode[];       // 可选：所有递归子节点（扁平化）
  relatedDependencies: GraphNode[]; // 与当前节点有依赖关系的其他节点
  involvedPersonnel: Person[];      // 当前层级涉及的所有人员
  parentChain: GraphNode[];         // 从根到当前节点的路径（提供上层背景）
  companyKnowledgeBase: string;     // 公司介绍等全局知识（始终可用）
}


示例：
  ∙	用户在 Level 0（全局）问 AI → AI 能看到所有部门的概况数据，但不含每个任务的细节
  ∙	用户钻入「技术开发」再问 AI → AI 只能看到技术开发部门下的项目列表、状态、人员，看不到市场运营的数据
  ∙	用户再钻入「Buddy V2 项目」→ AI 只看到这个项目的子任务、阻塞情况、相关人员 + 知道它属于技术开发部门（parentChain）
  ∙	但如果某个子任务依赖了市场运营部门的某个任务（跨部门依赖），那个被依赖的任务信息也会被包含在 relatedDependencies 中
AI 提示词中注入上下文
每次调用 AI 时，在 system prompt 中注入：

你当前正在查看的是：[面包屑路径]
该节点包含 [N] 个子任务，其中 [X] 个逾期，[Y] 个被阻塞。
涉及人员：[人员列表]
以下是该层级的详细数据：[JSON]
请基于以上数据范围来分析和回答问题。


视觉提示
  ∙	AI 对话框顶部显示当前上下文范围，如：“AI 正在查看：技术开发 > Buddy V2”
  ∙	如果用户问的问题超出当前作用域，AI 应提示：“这个问题涉及市场运营部门的数据，需要我切换到全局视角来回答吗？”
层级过渡时的物理模拟
  ∙	钻入新层级时，子节点从中心点向外做力导向扩散，所有之前实现的物理规则（排斥力、吸引力、阻尼、漂浮）在新层级中同样生效
  ∙	每个层级的物理参数可以独立，比如顶层部门级别的排斥力距离更大，底层任务级别的排斥力距离更小
  ∙	钻出时，当前层级的物理模拟停止，节点收缩回父节点位置
性能优化
  ∙	只对当前可见层级的节点运行物理模拟
  ∙	钻入子层级后，父层级的节点全部停止计算
  ∙	aggregatedStatus 在数据变更时预计算缓存，不要在每帧实时递归统计
  ∙	深层级节点数量可能很多，超过 100 个节点时考虑只渲染可视区域内的节点（viewport culling）


---

整体逻辑就是：**越高层级看到的是聚合后的"健康信号"，越深层级看到的是具体细节**。用户通过颜色和脉冲在宇宙视角快速定位哪个"银河系"出了问题，然后一路钻进去找到具体是哪个任务卡壳了。AI 的上下文也跟着视角走，避免塞太多无关数据进去浪费 token 还影响回答质量。

这个功能比较大，建议你让 Replit 分步实现：先做钻取动画和面包屑导航，再做视觉状态提示，最后做 AI 上下文作用域。​​​​​​​​​​​​​​​​
